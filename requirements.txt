# ===================== Web 框架 =====================
fastapi==0.111.0
uvicorn[standard]==0.38.0
pydantic==2.12.5
pydantic-settings==2.12.0
python-multipart==0.0.20

# ===================== 数据库 =====================
asyncpg==0.29.0

# ===================== LLM / Agent 基础设施 =====================
# Lead Agent（第二期）基于 langchain.agents.create_agent + AgentMiddleware 组装，
# 会话持久化经由 AsyncPostgresSaver（langgraph-checkpoint-postgres）。
langchain==1.3.9
langchain-core==1.4.7
langchain-openai==1.3.2
langchain_deepseek==1.1.0
langgraph==1.2.5
langgraph-checkpoint-postgres==3.1.0
# psycopg/psycopg-pool：langgraph-checkpoint-postgres 的运行时依赖，显式声明
# 版本避免上游静默升级改变连接池行为（main.py 里 Checkpointer 直接用到
# AsyncConnectionPool，见其初始化处的注释）。
psycopg==3.3.4
psycopg-pool==3.3.1

# ===================== Elasticsearch（长期记忆向量存储） =====================
elasticsearch==8.11.0

# ===================== 工具 =====================
python-dotenv==1.0.0
tavily-python==0.5.0
httpx==0.28.1
pyyaml==6.0
redis==5.0.1
# DockerSandboxProvider（SANDBOX_PROVIDER=docker 时使用，见 agent_core/sandbox/docker_sandbox_provider.py）
docker==7.1.0

# ===================== 日志 =====================
loguru==0.7.2

# ===================== 开发工具 =====================
pytest==7.4.3
pytest-asyncio==0.21.1
ruff==0.1.8
black==23.12.1
mypy==1.7.1
